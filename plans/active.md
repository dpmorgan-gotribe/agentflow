# Active Plans

<!-- AUTO-GENERATED MANIFEST — Updated by /plan-status and /plan-archive skills.
     Each entry: ID | Type | Status | Priority | Branch | Summary -->

| ID                                                                 | Type          | Status    | Priority | Branch                                              | Summary                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------------------------------------------ | ------------- | --------- | -------- | --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| feat-015-factory-extensions-post-mvp                               | feature       | draft     | P1       | feat/factory-extensions-post-mvp                    | Roadmap plans 9-13 (skills-audit auto-author, html-verifier, lessons-agent, register-mcp-build, git-agent alignment).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| feat-016-post-mvp-catalog-promotion                                | feature       | draft     | P2       | feat/post-mvp-catalog-promotion                     | Standardize post-mvp-scaffolding/ frontmatter + ship /post-mvp-review skill + run first 30-day cadence.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| feat-021-pm-agent-availability-and-requests                        | feature       | draft     | P2       | feat/pm-agent-availability-and-requests             | PM-side awareness + agent-change-request mechanism (analogous to kit-change-request). Phase A: PM reads `.claude/agents/`. Phase B: AgentChangeRequest schema. Phase C: orchestrator pre-Mode-B agent-fulfilment. Phase D: ship `agent-expert` with author-new-agent capability.                                                                                                                                                                                                                                                                                                                                                            |
| feat-034-devops-agent                                              | feature       | draft     | P1       | feat/devops-agent                                   | Ship `.claude/agents/devops.md` + 3 deploy stack skills (github-actions, vercel, fly-io) + models.yaml entry + PM/architect awareness. Closes the gap surfaced live on repo-health-dashboard-01: PM recruited `devops` but factory shipped no such agent → 4 tasks silently skipped.                                                                                                                                                                                                                                                                                                                                                        |
| feat-037-audit-reachability-ts-aware-rewrite                       | feature       | draft     | P2       | feat/audit-reachability-ts-aware-rewrite            | Replace regex-based `audit-app-reachability.mjs` with TS Compiler API or dependency-cruiser. Closes 5 structural gaps bug-030 Phase A patched surgically. Parent: bug-030.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| investigate-012-factory-readiness-pre-builds                       | investigation | approved  | P0       | -                                                   | Findings + Recommendation populated 2026-04-30. 8-dim rubric defined; 4 cross-cutting blockers confirmed (mock kind / Strategy C / webServer / node-fastify skill missing); 12-step validate-first roadmap; 5 follow-up factory plans named. Operator-locked decisions; advisory gating.                                                                                                                                                                                                                                                                                                                                                    |
| feat-043-build-to-spec-score-and-gating                            | feature       | draft     | P0       | feat/quota-observability                            | F6 of investigate-012. Extend /build-to-spec-verify to compute 8-dim score → docs/build-to-spec/score.json with verdict (≥95 ship-ready / 90-94 needs-itemized / <90 needs-major-revision). ADVISORY only per operator decision. Ships in parallel with feat-045 Phase E.                                                                                                                                                                                                                                                                                                                                                                   |
| bug-034-git-agent-additive-concat-merge-resolver-gap               | bug           | draft     | P0       | fix/git-agent-additive-concat-resolver              | Surfaced live on finance-track-01 launch (2026-05-01) — feat-transactions-crud emergency-aborted on additive same-region conflicts in `apps/api/src/app.ts` + `packages/types/src/index.ts`; cascade-aborted 10 dependent features. Phase A: ship `tryAdditiveConcatResolve` in git-agent (immediate). Phase B: per-feature route discovery in stack skills (structural). Phase C: `/orchestrator-state-fixup` skill. Project-side recovery in `projects/finance-track-01/plans/active/bug-002-...` (independent).                                                                                                                          |
| investigate-013-seed-state-coverage-from-brief                     | investigation | completed | P1       | -                                                   | COMPLETED in 18 min of 45-min time-box. Hypothesis (PM-side enumeration + stack-skill rule) FALSIFIED in better direction. Smoking gun at `orchestrator/src/invoke-agent.ts:1516` — `buildAgentPrompt` only includes `task.id` + `task.summary`, silently drops `task.notes` field. PM ALREADY emits state-coverage requirements in notes; reviewer reads them; builder doesn't. 5-line dispatch fix closes the bug class. Recommendation: ship bug-035, skip feat-046, defer Phase C stack-skill rule.                                                                                                                                     |
| bug-035-builder-dispatch-drops-task-notes-field                    | bug           | approved  | P0       | fix/builder-dispatch-includes-task-notes            | Filed from investigate-013 recommendation. 5-line edit to `orchestrator/src/invoke-agent.ts:1516` + regression test. Same root-cause shape as bug-024 (tester source-fix), bug-029, bug-031: incomplete context delivery in orchestrator MVP. Empirical case in `projects/finance-track-01/plans/active/bug-001-...` resolved as side-effect once dispatched (no manual seed.ts edit needed). Phase A: dispatch fix + regression test. Phase B: validate against finance-track-01 retry. Phase C: optional stack-skill rule (defer until residual variance observed).                                                                       |
| bug-036-parallel-checkout-feature-race-on-dirty-state              | bug           | draft     | P1       | fix/checkout-feature-serialize-dirty-state          | Surfaced live on finance-track-01 wave 4 resume (2026-05-01) — 3 features dispatched in parallel against dirty project root, 2 lost the race on `git`'s index lock during the dirty-state auto-commit + silently failed at checkout-feature with no worktree creation, no diagnostic in stdout. Phase A: per-project-root mutex around `runCheckoutFeature` (immediate). Phase B: pre-wave dirty-state commit (structural — eliminates race surface). Phase C: capture/surface checkout-feature stderr (defense in depth). Sister to bug-034 (orchestrator concurrency hardening).                                                          |
| bug-037-playwright-runtime-not-auto-installed-for-synthesized-e2e  | bug           | draft     | P0       | fix/auto-install-playwright-with-synthesized-e2e    | Surfaced on finance-track-01 verifier (2026-05-02) — `flow-execution: Cannot find module '@playwright/test'`. Synthesizer authored 9 spec files but apps/web/package.json never got `@playwright/test` in devDependencies. Verifier degraded gracefully → ALL E2E coverage was silently zero. Phase A: scaffold-time install via stack skill (immediate). Phase B: synthesizer auto-fix-up. Phase C: verifier hard-fail when specs exist + runtime missing. Phase D: playwright browser binary install strategy. Documented in react-next SKILL.md §3 since kanban-webapp-10 hit it; documentation isn't enforcement.                       |
| bug-038-parity-verify-backend-port-defaults-to-fastapi-8000        | bug           | draft     | P1       | fix/dev-server-stack-aware-port-resolution          | Surfaced on finance-track-01 verifier (2026-05-02) — `parity: dev-server: auto-boot failed: backend (apps/api/) did not respond on http://localhost:8000/health`. fastify backend was on port 4000 in `.env.local`; resolver defaults to 8000 (FastAPI). Three gaps: (1) doesn't read `.env.local` (bug-033 canonical), (2) doesn't consult architecture.yaml.tooling.stack.backend_framework for stack-aware default, (3) hardcoded "verify uv is on PATH" error misleads on non-FastAPI stacks. Phase A: extend resolution chain. Phase B: stack-skill canonical-port docs. Phase C: better diagnostic. Phase D: empirical re-validation. |
| investigate-014-fix-bugs-loop-parallelism-and-worktree-lifecycle   | investigation | completed | P2       | -                                                   | COMPLETED in 25 min of 60-min time-box. Both H1 (parallelism feasible via per-bug worktrees + bug-034 Phase A resolver) + H2 (worktree-remove failing on Windows file-lock — needs retry-with-backoff) confirmed. Empirical disk inventory: 1.2GB per project (less than 10GB hypothesis). Recommendation: ship feat-046 + feat-047 as paired P2 follow-ups, but DEFER behind higher-leverage work (bug-034 Phase B, bug-037 Phase B+C, bug-039 Phase B). No re-scope needed.                                                                                                                                                               |
| bug-039-verifier-schema-rejects-empty-flow-failure-screen-ids      | bug           | approved  | P0       | fix/flow-failure-screen-ids-nullable                | Surfaced on finance-track-01 verifier rerun (2026-05-02 post-bug-037+bug-038). Playwright DID run — got 9 flow failures back. But each failure had empty `fromScreenId`/`expectedScreenId` (v2.0 synthesizer emit path doesn't include the markers the runner's regex expects). Schema required `min(1)` → rejected entire `flows.failed[]` → 0 bugs filed → fix-loop reported `no-bugs` → 9 real product bugs sat silent in `apps/web/test-results/`. Phase A SHIPPED `e9cb267`: schema nullable + runner emits null + consumers null-safe + 3 regression tests. Phase B: synthesizer v2.0 metadata embedding (defer).                     |
| feat-046-fix-bugs-loop-per-bug-parallelism                         | feature       | draft     | P2       | feat/fix-bugs-loop-per-bug-parallelism              | investigate-014 follow-up. Per-bug worktree dispatch via `Promise.all` batches of `--max-concurrent`; per-iteration sequential merge-cascade leveraging bug-034 Phase A resolver for additive same-region conflicts (8-shared-file pattern empirically observed in finance-track-01 fix-loop iter 1). Wall-clock estimate: 50min sequential → 10-15min parallel at cap=5. ~2 dev-days. Pair-ship with feat-047 to keep transient disk amplification bounded.                                                                                                                                                                                |
| feat-047-worktree-auto-prune-on-close-feature                      | feature       | draft     | P2       | feat/worktree-auto-prune-on-close-feature           | investigate-014 follow-up. git-agent close-feature handler attempts `git worktree remove --force` + `git branch -d` post-merge with 5-retry exponential backoff (handles Windows "Directory not empty" file-lock issue documented in F5). `--keep-last-n-worktrees` operator flag for forensic-debug retention. New `/cleanup-worktrees` skill for retroactive cleanup. ~1.25 dev-days. Disk saving: ~80-90% per finished feature. Pair-ship with feat-046.                                                                                                                                                                                 |
| bug-044-flow-3-tohaveurl-regex-vs-absolute-url                     | bug           | draft     | P2       | fix/flow-3-tohaveurl-absolute-url                   | Wave 2 follow-up surfaced 2026-05-03 once bug-040/041/042/043 fixed the seeding pipeline. Hand-written flow-3 spec passes 6 steps then fails at step 7: `expect(page).toHaveURL(/^\//)` regex never matches the absolute URL `http://localhost:3000/`. Test-authoring bug; was masked behind step 2's empty-state symptom pre-Wave-2. Likely fix: change to `toHaveURL("/")` (Playwright resolves relative to baseURL). project-side, no factory work.                                                                                                                                                                                      |
| bug-045-synthesized-flow-3-display-currency-selector-mismatch      | bug           | draft     | P2       | fix/synthesizer-selector-inference-display-currency | Wave 2 follow-up surfaced 2026-05-03. Phase A diagnosis (2026-05-03): NOT a synthesizer issue — manifest correctly authored against design; the BUILD diverged by omitting the topbar currency selector. Phase B decision: defer to /build-to-spec-verify's fix-loop (canonical surface for build-completeness gaps). Phase C: empirical re-validation post fix-loop.                                                                                                                                                                                                                                                                       |
| feat-048-synthesizer-output-linting                                | feature       | draft     | P2       | feat/synthesizer-output-linting                     | Defense-in-depth follow-up to bug-046+047. Pre-emission lint pass on synthesized specs: TS typecheck (`tsc --noEmit`) + Playwright locator dry-create (parses each `page.locator(<selector>)` selector before runtime). Catches future classes of synthesizer-output bugs that haven't surfaced yet — saves ~30s × N flows × M retries of verifier wall-clock per round. Defer until bug-046+047 land + observed for 1 project's worth of cycle.                                                                                                                                                                                            |
| bug-048-audit-reachability-js-extension-not-resolved               | bug           | draft     | P1       | fix/audit-reachability-js-extension-resolution      | Surfaced 2026-05-03 on finance-track-01 verifier rerun (post bug-046+047). 4-of-5 orphan-component reports against `apps/api/src/**.ts` are false positives caused by `resolveCandidate()` not handling TS-as-ESM `.js → .ts` suffix swap (`from "../common/errors.js"` never resolves to `errors.ts`). Affects every TS-as-ESM project (node-fastify + similar). Phase A: narrow regex extension in `resolveCandidate`. Phase B: fixture-driven regression test. Phase C: empirical re-validate against finance-track-01. Sister to bug-049; long-term cure is feat-037.                                                                   |
| bug-049-audit-reachability-config-string-references                | bug           | draft     | P2       | fix/audit-reachability-config-string-refs           | Surfaced 2026-05-03 on finance-track-01 verifier rerun. 5th orphan: `apps/web/playwright/global-setup.ts` referenced by `playwright.config.ts:20 globalSetup: "./..."` (string property, not import). `IMPORT_RE` only matches `from "..."` / `import("...")` / `export ... from "..."` — config-string property values invisible. Phase A: complementary `CONFIG_STRING_PATH_RE` for relative-path string literals ending in source ext. Phase B: fixture test. Phase C: combined re-validation. Sister to bug-048.                                                                                                                        |
| bug-050-verifier-failure-class-taxonomy-and-agent-routing          | bug           | draft     | P1       | fix/verifier-failure-class-taxonomy-and-routing     | Surfaced 2026-05-03 on finance-track-01 9-flow E2E run. `primaryCause` taxonomy collapses 3 distinct classes (build-gap, manifest-author, seed-mismatch) into `step-transition`; `defaultAgentSequence()` does `void violation;` and routes everything to web-frontend-builder. 5/9 finance-track-01 failures would dispatch to wrong agent. Phase A: extend primaryCause enum (deps feat-049 discriminator). Phase B: route agentSequence by class. Phase C: schema + tests. Sister to feat-049/050/bug-051; closes the autonomous-loop dispatch correctness gap.                                                                          |
| feat-049-screens-json-cross-reference-for-build-gap-classification | feature       | draft     | P1       | feat/screens-json-cross-reference                   | Discriminator that lets bug-050's taxonomy distinguish build-gap (design intended X, build missed it) from manifest-author (design didn't intend X, flow hallucinated). Builds an in-memory ScreensCatalog from docs/screens/\*.json + mockup HTML DOM walk; runner cross-references failing selectors. Phase A: catalog builder. Phase B: classifySelector helper. Phase C: plumbing. Phase D: /screens SKILL.md contract tightening. Empirical target: re-run on finance-track-01 produces 4 build-gap + 3 seed-mismatch + 2 manifest-author per bug-050's table.                                                                         |
| feat-050-per-flow-seed-orchestration                               | feature       | draft     | P1       | feat/per-flow-seed-orchestration                    | Closes the seed-mismatch class (3/9 finance-track-01 failures: flow-1 expects empty, flow-8 different name, flow-9 stale fx). Manifest schema gains `requiredState: { kind: "baseline"\|"empty"\|"custom", tablesToCleanup, fixtures }`. Synthesizer emits per-flow beforeAll calling existing /test/cleanup + /test/seed (bug-042 contract). Phase A: schema. Phase B: synthesizer emission. Phase C: backend partial-fixture audit. Phase D: SKILL.md guidance. Phase E: empirical re-validate against finance-track-01 flows 1+8+9. Closes bug-042's deferred selector→table-inference follow-up.                                        |
| bug-051-user-flows-generator-selector-quality-and-mocking-strategy | bug           | draft     | P2       | fix/user-flows-generator-selector-quality           | Closes the manifest-author class (2/9 finance-track-01 failures: flow-2 `:has-text` strict-mode violation, flow-4 page.route mocks browser but call originates from backend). /user-flows-generator SKILL.md doesn't warn about either trap. Sister to bug-046+047 (same defense-in-depth shape: SKILL.md anti-pattern + synthesizer post-flight regex lint). Phase A: SKILL.md callouts. Phase B: HAS_TEXT_AMBIGUOUS_RE post-flight. Phase C: backend-mock warning. Phase D: regression tests. Reduces the manifest-author rate at the source.                                                                                             |

<!--
ARCHIVED 2026-05-03 (Wave 2 follow-up — synthesizer hardening; commits c0ddd7c / 6b0f46a):
- bug-046 user-flows-generator-malformed-css-role-selectors (P1, success) — Phase A+B+D shipped (6b0f46a). /user-flows-generator was authoring invalid Playwright selectors mixing CSS with `role=` engine via SPACE instead of `>>`. SKILL.md §4b expanded with explicit anti-pattern callout + ❌ wrong / ✓ right / ✓ better examples + selector preference re-ordered. Synthesizer post-flight regex-detect (ENGINE_MIX_RE) pushes hard error to errors[] when mid-selector ` role=`/`text=`/`xpath=`/`id=`/`data-testid=` appears not preceded by `>>`. No auto-rewrite — synthesizer stays mechanical. Empirical: finance-track-01 manifest had 10 instances; hand-fixed to clean (Phase D project commit 16be3ec); synthesizer post-flight now emits `errors: []`. +4 regression tests.
- bug-047 synthesizer-tohaveurl-path-shape-pattern-mismatch (P1, success) — Phase A+B+C+D shipped (6b0f46a). Synthesizer `case "assertUrlMatches"` was emitting `expect(page).toHaveURL(new RegExp(pattern))` with manifest patterns authored as path-shape (`^/foo`); Playwright matches full URL so `^`-anchored path never matched. SKILL.md §4b §dev-step-5 documents path-shape→URL-shape rewrite contract with edge-case examples. New `rewritePathShapeToUrlShape` helper rewrites `^/foo` → `^https?://[^/]+/foo`; unanchored / URL-shape patterns preserved verbatim. Empirical: finance-track-01 flow-7 progressed past interaction 3 (toHaveURL on `/reports`) — page snapshot shows `Reports [active]` proving URL assertion now passes. Sister fix to bug-044 (hand-written occurrence). +5 regression tests.
- Tests: orchestrator 620 → 629 (+9 across both bugs).

ARCHIVED 2026-05-03 (Wave 0/1/2 — 5-link seeding-pipeline failure chain empirically fixed; commits 78d8038 / ec1af9e / 5a69eee / b5ebb07 / 15b7d28 / a5a13f9):
- bug-040 architect-skips-scripts-dev-mjs-emission-step (P0, success) — Phase A.5+B shipped (5a69eee). Renamed `dev-multi-tier.mjs.template` → `-python-fastapi.mjs.template`; authored `-node-fastify`/`-node-trpc-nest`/`-node-express` siblings. Each backend SKILL.md gained §dev-orchestrator pointer. architect/SKILL.md §7c rewritten with decision-table-driven template resolution by `architecture.yaml.tooling.stack.backend_framework` slug; hard-fails on unknown slug. architect.md self-verify item 13 auto-fixes missing scripts/dev.mjs for multi-tier projects. ArchitectOutputSchema gained `scaffoldedFiles[]`. Phase C deferred (defense-in-depth; not load-bearing per Wave 2 success).
- bug-041 playwright-config-missing-webserver-block (P0, success) — Phase A+B+C shipped (b5ebb07). Synthesizer (scripts/synthesize-flow-e2e.mjs) gained `errors[]` output + post-flight check that reads playwright.config.ts content + asserts `webServer:` substring present; orchestrator/src/build-to-spec-verify.ts surfaces synth errors[] + warnings[]. react-next + svelte-kit SKILL.md §3a restructured: webServer block lifted into top-level "§3a.1 Required playwright.config.ts template — COPY VERBATIM" subsection with stack-keyed decision table. web-frontend-builder.md self-verify item 6 auto-fixes missing webServer. Phase D deferred. +3 orchestrator tests (617 → 620).
- bug-042 global-setup-baseline-only-seeds-fx-cache (P0, success) — Phase A.5+A+B+C shipped (15b7d28). Uniform `/test/seed-baseline` endpoint contract added to all 3 shipped backend SKILL.md (node-fastify, python-fastapi, node-trpc-nest); endpoint wraps the project's canonical db/seed.{ts,py}. testing-policy.md gains §Strategy-C-test-seed-contract. Synthesizer emits `apps/web/playwright/required-baseline.json` signal for Strategy C projects. playwright-global-setup.ts.template + seed-db.ts.template rewritten to read signal + POST /test/seed-baseline. Phase D deferred. Selector→table inference deferred as feat-future depth (signal is enough).
- bug-043 orchestrator-dev-server-spawn-command-fastapi-only (P0, success) — Phase A+B+D shipped (ec1af9e), filed mid-session as sister to bug-038/040. STACK_BACKEND_SPAWN_COMMAND lookup table keyed by backend_framework; resolveBackendSpawnSpec helper; spawnBackendDevServer refactored. bootDevServer error names actual spawn command + per-stack hint via HINTS_BY_SLUG. Shared readBackendFrameworkSlug helper extracted between bug-038 + bug-043. +10 orchestrator tests (607 → 617). Phase C (per-stack §dev-server-spawn docs) deferred — covered by bug-040's §dev-orchestrator pointers.
- Empirical Wave 2 validation against finance-track-01 (uncommitted to factory git per `agenticVisibility: private`): scripts/dev.mjs (node-fastify variant) + playwright.config.ts webServer block + apps/api/src/routes/test-seed.ts /seed-baseline route + apps/web/playwright/global-setup.ts (calls /seed-baseline) all patched. Backend boots on :3001, frontend on :3000, /test/seed-baseline=204, /api/accounts=3 active accounts (USD/GBP/JPY), /api/transactions=174+ rows. Hand-written flow-3 reaches step 7 before failing on URL-regex authoring bug (vs step 2 "Card not found" pre-fix). Synthesized flow-3 reaches interaction 4 before failing on Display-currency-button selector mismatch (vs interaction 2 "No accounts yet" page snapshot pre-fix). Page snapshot confirms populated dashboard ("May 2026 · normalized to EUR" with full nav).

ARCHIVED 2026-05-01 (investigate-012 roadmap session — 5 features + 1 bug closed in single session):
- feat-039 mock-interaction-step-kind (P0, success) — F1 of investigate-012. Added `kind: "mock"` to InteractionStep union (zod + JSON schema); extended synthesizer to emit page.route() with method-check + fulfill; new strategy-d-with-mock fixture; ordering test (mock BEFORE navigate). Single attempt; 404 contracts + 578 orchestrator tests passing.
- feat-040 live-backend-playwright-webserver (P1, success) — F2. react-next + svelte-kit SKILL.md §Testing now emit `webServer.command="node ../../scripts/dev.mjs"` for multi-tier (Strategy C/D); `pnpm exec next dev` for Strategy A. Retries bumped 0→1 for live-backend specs. Synced to all 12 react-next consumers. De facto pre-shipped on repo-health-dashboard-01 via feat-045 hand-fix.
- feat-041 node-trpc-nest-strategy-c-testing-block (P1, success) — F3a. Added Strategy C declaration + `/test/seed` + `/test/cleanup` Nest controller contract + ENABLE_TEST_SEED env gate to node-trpc-nest §3 (mirroring python-fastapi §3 1:1). Unblocks book-swap Mode B at backend-builder dispatch.
- feat-042 node-fastify-stack-skill (P0, success) — F3b. Authored `.claude/skills/agents/back-end/node-fastify/SKILL.md` from scratch — 8 sections (layout, idioms, testing+Strategy C, commands, gotchas, review, deps, anti-patterns, self-verify). Synced to finance-track-pre-build + finance-track. Unblocks finance-track Mode B.
- feat-044 shepherd-kanban-webapp-09-to-95 (P0, abandoned) — kanban-09 had 100+ uncommitted source modifications + 18 broken unit tests (label-rename refactor stalled mid-flight). Phase A+B authored 10 flows + synthesizer ran clean (48 Playwright passed); Phase C blocked by project-state. Operator pivoted to repo-health-dashboard-01 instead. Lessons captured (drag-drop / dblclick / file-upload missing from v2.0 schema; pre-flight uncommitted-state check needed; isomorphic-dompurify SSR footgun in Next 15+). Superseded by feat-045.
- feat-045 shepherd-repo-health-dashboard-01-to-95 (P0, success) — First validation target. Final score: 90/100 (manual-sanity 0/10 deferred). All measurable dims full marks: Mode A 10/10, Mode B 15/15, Reachability 10/10, Parity 15/15, Flow E2E 20/20 (8/8 specs pass in 13.3s deterministically), Coverage 10/10 (web 95.29%, api 97%), Verifier 10/10. Surfaced + closed 2 factory-wide bugs mid-flight (bug-033 dev.mjs env propagation, bug-119-class testing-policy hardening). Rubric proven on Strategy D end-to-end.
- bug-033 dev-mjs-env-not-propagated-to-fastapi (P0, success) — surfaced live during feat-045 Phase B. scripts/dev.mjs now loads .env + .env.local into the subprocess env via parseEnvFile + loadEnvFiles. GITHUB_TOKEN reaches FastAPI; live /api/report/facebook/react returns 200 with rate_limit.remaining=4998 (authenticated 5000/hr). Synced to all 12 projects. Testing-policy.md hardened (proxy/external-API tests must mock).

ARCHIVED 2026-04-30 (feat-038 complete — synthesize-flow-e2e v2.0 chain end-to-end):
- feat-038 deepen-synthesize-flow-e2e-and-data-seeding (P0, success) — 6-phase feature shipped one session. Phase 0: per-stack-skill seeding strategy decision in testing-policy.md. Phase 1: v2.0 user-flows-manifest schema (Zod + JSON + 24 tests). Phase 2A: synthesizer interactions[] translator (10 InteractionStep kinds). Phase 2B: per-strategy seed-helpers (4 templates) + persistence_layer field + architect skill §7d. Phase 3: /user-flows-generator authors interactions[] + seedingTier (selector inference algorithm). Phase 4: parseFailureMessage v2.0-aware + seed-setup primaryCause + classifier ladder. Phase 5: fixture-driven validation harness (3 strategies × 6 tests). Empirical validation on repo-health-dashboard-01: synthesized flow-1 spec failed cleanly at interaction 4 ("page.waitForResponse: Test timeout") proving the kanban-09 / repo-health-01 class integration bug is now caught (legacy v1.0 spec would have passed silently). 374→398 contracts (+24) + 568→576 orchestrator (+8) tests passing. book-swap is the next concrete consumer when its E2E layer lands (Strategy C real-DB).

ARCHIVED 2026-04-30 (sync-project-schemas extension, post-feat-038-Phase-2B):
- refactor-008 sync-project-schemas-rules-templates (P2, success) — extended scripts/sync-project-schemas.mjs to cover .claude/rules/ + .claude/templates/ via a recursive walker + 2 new SYNC_PAIRS entries + mkdirSync guard for nested file parents. Closes the manual factory→project copy tax (30 manual `cp` invocations this session alone). Single attempt; validated via dry-run against book-swap-pre-build (in-lockstep, 41 unchanged) + reverse-drift on kanban-webapp-09 (1 update + 18 creates including 5 nested ui-kit-eslint-plugin/ files preserved). Live --all run synced 12 projects. 568/568 orchestrator tests still pass.

ARCHIVED 2026-04-30 (Zod v4 typing migration, post-feat-038-Phase-1):
- refactor-007 zod-infer-types-tests (P2, success) — migrated 4 orchestrator-contracts test files (bugs-yaml / build-to-spec-verify / parity-verify / screen-fixtures) from `typeof Schema._type` (Zod v3) to `z.infer<typeof Schema>` (Zod v4). 15 occurrences, single attempt, mechanical. Closed the typecheck-red / tests-green split (vitest masks tsc errors via esbuild). 568/568 orchestrator + 398/398 contracts tests still pass.

ARCHIVED 2026-04-30 (verify+fix-loop end-to-end on repo-health-dashboard-01):
- bug-030 audit-reachability-false-positive-flood (P1, success) — Phase A surgical fixes to `audit-app-reachability.mjs`: drop `packages/` from SCAN_ROOTS, prepend `apps/web` to `@/` alias roots, extend `IMPORT_RE` for `export … from`. Empirical false-positive count 62 → 0 on repo-health-dashboard-01; regression-tested with synthetic orphan. Structural follow-up filed as feat-037 (TS-aware rewrite).
- bug-031 fix-loop-fixup-worktree-not-seeded (P0, success) — Phases A+B+D shipped: exported `seedWorktree` from invoke-agent.ts, `openFixupWorktree` now invokes it after `git worktree add` and re-runs idempotently on pre-existing worktrees, regression test asserting hooks + autonomous `permissions.allow` post-conditions. 568/568 orchestrator tests pass. Empirically validated on repo-health-dashboard-01: 2 parity bugs resolved through full builder→tester→reviewer chain in 1 attempt each.
- bug-032 api-base-url-not-coordinated-with-backend-port (P0, success) — Phases A+B+C shipped. Phase A: per-project `.env.example` files; Phase B: `scripts/dev.mjs` port-coordinated dev orchestrator (smoke-tested through 4 iterations of fixes); Phase C: factory-level — `dev-server.ts` co-boots backend during verify auto-boot, architect skill scaffolds env contract + `dev-multi-tier.mjs.template`, python-fastapi + react-next stack skills declare env contract as canonical. Phase D (synth-flow baseURL signal) deferred to feat-038. 568/568 orchestrator tests still pass.

ARCHIVED 2026-04-27:

Bugs (validated by kanban-webapp-09 + -10 runs):
- bug-002 worktree-missing-hooks-perms
- bug-003 builder-output-contract-mismatch
- bug-004 agent-output-format-schema
- bug-005 windows-quoting-and-default-branch
- bug-006 greedy-json-extractor (→ superseded by bug-007 in plans/superseded/)
- bug-007 robust-output-extraction
- bug-008 close-feature-dirty-root
- bug-009 checkout-feature-snapshot
- bug-010 graceful-skip-unknown-agent
- bug-011 ship-security-agent
- bug-012 lockfile-aware-conflict-resolution
- bug-013 feature-context-gitignore
- bug-015 parallel-feature-source-contention (3 phases)
- bug-016 pre-flight-snapshot-race (TOCTOU race in concurrent close-feature)
- bug-017 verify-wiring-and-surfacing (factoryRoot + cli-runner result.verify) — completed inline

Investigations (recommendations implemented):
- investigate-004 agent-shipped-vs-task-gap → bug-010 + bug-011
- investigate-005 gitignore-audit → bug-014 (folded into bug-013 + .gitignore)
- investigate-006 build-to-spec-verification → feat-022 + feat-023
- investigate-007 orchestrator-liveness-and-pause → feat-024
- investigate-008 build-to-spec-verify-not-firing → bug-017 (factoryRoot + result.verify surfacing)

Features (validated by kanban-09/10 end-to-end):
- feat-014 mvp-completion-autonomous-e2e — MVP EXIT ACHIEVED (10/10 features, $45.03 on -09)
- feat-017 auth-provider-config — claude-max-subscription default working
- feat-018 mode-b-commit-discipline — auto-commit per task working
- feat-019 builder-install-discipline — pnpm install on package.json change working
- feat-022 build-to-spec-verification — post-Mode-B reachability + flow-E2E synth + auto-bug-plans
- feat-023 pm-stage-brief-coverage-assertion — PM gate on silent capability drops
- feat-024 orchestrator-pause-resume — SDK AbortController + keepalive watcher + /pause-build + /resume-build
- feat-025 flow-spec-execution — Phase 2 of feat-022 (actual spec execution + Playwright install-discipline + bug-plan auto-author); 644 tests passing

ARCHIVED 2026-04-28 (verifier suite expansion; commits 9622ad3 + ce00f41):
- feat-027 runtime-error-capture — Playwright runtime listeners + cascade-root bug routing + bugPriorityComparator promotion; 4 phases shipped in single agent pass
- feat-028 visual-parity-verifier — DOM-diff via data-kit-* + computed-style audit; 7-pattern classifier; one bug per (screen, pattern). Gap-fix folded into feat-029
- feat-029 screen-state-fixtures — closes feat-028 empty-app blind spot; hybrid auto-derive (Pattern A) + flow-context (Pattern B) fixture system + dev-only seed-from-URL helper + parity-verify SKILL.md gap closed
- Combined: 693 → 888 tests (+195)

ARCHIVED 2026-04-28 (PM affects_files; commit ffeac54 + project-side syncs):
- bug-018 pm-skips-affects-files — original framing was "PM confabulation" but empirical follow-up revealed the load-bearing root cause is factory→project schema drift. SKILL.md §0 + step 4b strengthening shipped (PMs now resync schema or file a bug instead of silently skipping); both re-PM agents (finance-track, kanban-webapp) achieved 100% affects_files coverage on retry. Spawned bug-019 for the underlying schema-sync mechanism.

ARCHIVED 2026-04-28 (PauseSignal propagation; commit 88c5b32):
- bug-022 pause-signal-swallowed-in-sdk-hooks (P0, success) — runLlmAgent's SDK hook catches (rate-limit / auth-failed / stall) plus the outer for-await catch swallowed PauseSignal, causing the agent to complete past the pause + the next iteration's poll to overwrite the original cause. Fix: re-throw PauseSignal from each catch (4 sites). Discovered while resuming repo-health-dashboard-01 — bug-021 fix worked correctly + revealed bug-022 as the next blocker. 555 orchestrator + 344 contracts passing (+3 tests).

ARCHIVED 2026-04-29 (rate-limit observability + prompt caching; commits c4e6c0f / 8b8351d / 1c05176 / fd71257):
- investigate-010 rate-limit-observability-and-reduction → feat-030 + feat-031
- feat-030 quota-observability (P1, success) — /quota-status skill + 1-token SDK probe (Haiku/Sonnet/Opus via --model or --all) + rate-limit-events.ndjson ledger + warning-vs-rejection gate split + per-model cost/cache breakdown in counters.json. 555 → 567 tests (+11). Live-validated via repo-health-dashboard-01 resume — first reviewer dispatch wrote rate_limit_event{status:allowed} to the ledger within 30s.
- feat-031 prompt-cache-systemprompt (P2, partial) — buildAgentOptions now passes `systemPrompt: { type: 'preset', preset: 'claude_code', excludeDynamicSections: true }`. Cacheable prefix cross-agent. Estimated 30-50% input-token cut on dispatches 2-N. Plan's elaborate buildSystemPromptArray() helper unnecessary — agents read SKILL.md via tools at runtime, not pre-loaded. A/B validation deferred; first post-feat-031 Mode B run will surface cache-hit ratio automatically via feat-030 §D modelBreakdown.cacheReadInputTokens.

ARCHIVED 2026-04-29 (DAG observability + idea bucket; commits ae0d19f / 33974be / 0dd2446):
- feat-032 dag-status-skill (P2, success) — /dag-status skill renders the feature DAG with state markers ([DONE]/[FLOW]/[NEXT]/[WAIT]/[FAIL]/[ABRT]) + per-feature dependency edges + cumulative spend + per-model cost+cache-hit breakdown. Live-validated against repo-health-dashboard-01 mid-run. Phase B ETA forecast deferred (needs ≥3 historical runs).
- feat-033 idea-bucket (P2, success) — /idea + /idea-list + /idea-promote skills for lightweight capture of half-baked thoughts to docs/ideas.md. Pure declarative SKILL.md (no helper scripts). Phase D periodic-review nudge deferred until empirical signal of stale-pile accumulation.

ARCHIVED 2026-04-29 (live repo-health-dashboard-01 cycle — Phase B parity-verify + 7 bugs + 1 feat; multiple commits):
- feat-035 visual-parity-v2-built-page-render (P1, success) — Phase B Playwright driver shipped; standalone CLI added; live-validated. See plans/archive/feat-035-*.md for the deferred items per §Non-goals.
- bug-023 vitest-config-merge-thrash (P1, success) — three-layer scaffold-owned protection: stack-skill §Files NOT to modify, web-frontend-builder §Hard rules guard, SCAFFOLD-OWNED comment header on initial scaffold.
- bug-024 tester-modifies-source (P1, success) — three-layer hard constraint: tester.md §Hard constraint, testing-policy.md §Genuine product bug — CONSTRAINT, tester SKILL.md mirror. Empirically validated on the run that motivated it.
- bug-025 cross-feature-url-contract (P1, success) — four-layer URL contract: schemas/screens.schema.json `routePattern` field, react-next + svelte-kit §2.5 Routing Contract, web-frontend-builder §Hard rules nav-URL guard, PM SKILL.md §2c surfacer.
- bug-026 api-client-import-extensions (P1, success) — three-layer cross-tier package conventions: project hotfix dropping `.js` extensions, back-end stack-skills §6.5 Cross-tier package conventions, web-frontend-builder §Hard rules flag-don't-fix note.
- bug-027 fix-bugs-loop-auto-merge-fails (P1, success) — closeFixupWorktree reorder (remove worktree before merge) + surface warning instead of silent catch{}.
- bug-028 audit-reachability-misses-router-push (P1, success) — recursive dynamic-segment strip for template-literal matching + SCAN_ROOTS expansion (apps/web/components, apps/web/lib, packages/* now scanned). Validated 3 false-positive orphans → 0.
- bug-029 ui-kit-primitives-missing-data-kit-component (P1, partial) — Phase A (/stylesheet auto-retrofit on new projects) + Phase D (web-frontend-builder defensive flag-don't-fix note) shipped. Phases B (bulk retrofit script) + C (visual-parity bug-fix loop routing) deferred.

ARCHIVED 2026-04-29 (Mode B wall-clock investigation; commit 60c298e):
- investigate-011 mode-b-wall-clock-reduction (P1, success) — research-only. Identified concurrency cap default 4 (matches observed fan-out), agent_sequence is strict-serial loop (security+tester COULD parallelize on read-only access), all-tasks-bundle-into-one-builder-dispatch with no parallel-tool-use guidance, no per-task model override schema field, feat-019 pnpm install heuristic already short-circuiting correctly. Top 3 recommended levers (combined ~30-45% wall-clock reduction): (1) per-task parallelism via prompt + SKILL.md guidance for parallel tool_use; (2) Promise.all of read-only tester+security; (3) per-task model tiering for mechanical work (scaffold, gha-ci, deploy-config) → Haiku. Cache-warmup, pnpm-symlink, reviewer-fast-pass, speculative-merge all deferred (low payoff or high risk). Recommendation-implemented-by: feat-035 + feat-036 + feat-037 (to be authored).

ARCHIVED 2026-04-28 (resume-path correctness + schema-sync; commits 3561240 / afb7dee / f73e5f0):
- bug-021 checkout-feature-no-worktree-reuse (P0) — orchestrator's runFeature unconditionally dispatched checkout-feature on resume, hard-failing with stale-worktree. Three-gap fix: (1) createProgressTracker(seedSnapshot) hydrates inFlight[] from disk; (2) cli-runner --resume-feature-graph reads feature-graph-progress.json + threads through ctx.seedProgress; (3) runFeature detects in-flight, skips checkout, walks from nextAgent. +8 tests; 552 orchestrator + 344 contracts passing.
- bug-020 recovery-discards-completed-builder-work (P1) — replaced /resume-build SKILL.md §7 dirty-builder soft-reset with universal commit-and-advance; classes split by nextAgent (dirty-advance / dirty-final). Operator note covers the rare mid-execution edge case. Layer 3 timestamp work deferred. Outcome: partial (Layer 1 ships now; Layer 3 follow-up bug if needed).
- bug-019 new-project-force-schema-sync (P1) — authored scripts/sync-project-schemas.mjs; wired into /new-project --force step 5a (BOTH init + refresh modes). Idempotent byte-compare; --dry-run + --all flags. pm SKILL.md §0 cross-refs the script as the right answer to "field missing from schema". Synced all 12 projects on 2026-04-28.
-->
